{
  "name": "wallet-api",
  "version": "0.1.0",
  "description": "Minimal API used to exercise the container + CI pipeline",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
